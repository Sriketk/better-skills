{
  "name": "example-skill-server",
  "version": "1.0.0",
  "description": "Example skill server deployment",
  "type": "module",
  "scripts": {
    "start": "tsx ../../packages/server/src/cli.ts --dir ./skills --name vercel-skills --author Vercel --port 3001",
    "dev": "tsx watch ../../packages/server/src/cli.ts --dir ./skills --name vercel-skills --author Vercel --port 3001"
  },
  "dependencies": {
    "@better-skills/server": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.7.0"
  }
}